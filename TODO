konv# RaySession TODO

    Jack Patchbay
    drop file directly on session
    manual
    ...
    
    review list with net and last reply -> trop compliqué pour pas gd chose
    problème fenêtre network s'ouvre au dessus de l'autre
    IGU ray-network mal initialisé
    ray-network devrait switcher même juste après création
    
    find something better than check executable_path='ray-network'
    
    Session sous NSM non renommable
    
    send session is loaded when program ready
    
    proxy sans qt (pour légèreté), l'IGU totalement intégrée à RS, configurable même si proxy arrêté (si faisable).
    proxy manage himself variables changes concerning files
    
    MEGA DOC scripts
    
    unable to use wmctrl #check
    
    import warnings
    warnings.filterwarnings("ignore", category=DeprecationWarning)
    
    Tuer le script de session si user decheck l'option
    

