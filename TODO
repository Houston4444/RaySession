konv# RaySession TODO

    Jack Patchbay
    drop file directly on session
    manual
    allow scripts
    ...
    
    review list with net and last reply
    if client sends error while loading, dont prevent server status to become ready
    prevent daemonCrash window at clean daemon stop
    
    find something better than check executable_path='ray-network'
    voir le souci git tag
    copie réseau trop longue
    voir ce qu'on fait avec les aborted
    enregistrer le temps de réponse du client pour l'attendre au chargement de la session
    nsm_locked dans le multi-daemons.xml
    
    option stop devrait stopper tous les démons
    option --port
    option --hidden
  
