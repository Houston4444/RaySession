#!/usr/bin/env python3

import sys
from pathlib import Path
from typing import TYPE_CHECKING

sys.path[0] = str(Path(__file__).parents[1] / 'clients' / 'sooperlooper')

if TYPE_CHECKING:
    import clients.sooperlooper.sooperlooper
else:
    import sooperlooper
