revoir group positions
review icon managing group type
review canvasicon, many things to remove
voir : canvas initial_pos

trigger_disconnect portgroup


finish Silver Gold

transport controls


refaire theme gui hidden tous les thèmes
ajustements anti-superposition (après join/split, emballer/déballer remonte toujours un peu)

utiliser un logger nottament pour les chargements de thème
#utiliser Path pour le chargement du thème
Commenter plus
#enlever les qcritical -> logger
Bug: connection non sélectionnée après clic droit

self._hover_item voir une soluce pour que VsCodium le trouve
    trigger_disconnect

    Classe mère pour tout qgraphicsitem
    Classe pour connectable (Port ou portgroup)
    Classe pour triggerable (port, portgroup ou connection)

    méthodes dans la scene pour itérer que certaines classes, puisque elle peut importer tous les types

revoir deplace_repulser.. (scene) avec des objets (plus facile d'identifier les types)
    Probablement séparer fonction (elle fait déjà 300 lignes)